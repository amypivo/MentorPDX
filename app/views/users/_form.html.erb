<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :availbilty %><br>
    <label>Monthly</label>
    <%= check_box(:user, :availbilty, {:multiple => true}, 1, nil) %> <br>
    <label>Bi-Weekly</label>
    <%= check_box(:user, :availbilty, {:multiple => true}, 2, nil) %> <br>
    <label>Weekly</label>
    <%= check_box(:user, :availbilty, {:multiple => true}, 4, nil) %> <br>
  </div>
  <div class="field">
    <%= f.label :city %><br>
    <%= f.text_field :city %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
